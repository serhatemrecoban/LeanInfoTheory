# Review Tooling Provenance

This maintained local adaptation comes from Serhat Emre Coban's
PerfectFunctionalRepresentationsLean project, under Apache-2.0 (see the root
[LICENSE](../../LICENSE)). Existing source notices and the fixture-only
qualifications are retained; no manuscript or live PFR record is included.

Source: the PFR-C02 current-working-source export captured 2026-09-11 at
10:42:25 UTC, based on HEAD `54c1714d1a9a891939d6c9f7d38c0d05896b2c75` plus
the exported uncommitted files, not an older Git checkout. Archive SHA-256:
`d776b4851b336875d09631fbd1a533f6e49cd1ff1525cbf1dcffe3633aaea3b2`.

The export's inventory and provenance records distinguish the working core and
review documents, matching C02's sampled first/final fingerprints, from later
AGENTS.md guidance on justified technical and advisory-plan adjustments. The
latter discretion is preserved without changing frozen approved requirements.
Those samples do not prove every intervening version or unattended behavior.

`core/`, including its maintained tests and three static fixture inputs, is
retained byte-for-byte. Its historical PFR/toy/DIAGNOSTIC labels are fixture
provenance, not library approval or live reviewer identities. Its default path,
fixture, evidence, finding-history and closure guards are not relaxed.

`project.py`, `project_source.py`, `cli.py`, and adapter tests are adapted for
LeanInfoTheory ownership, source/configuration, chunk IDs, the current native
subagent schema, named-message selection and library instructions.
`library.py` supplies the rubric and existing-validator guidance embedded in
every generated request. The two review documents and tooling README are
adapted operating instructions, not PFR execution records. No new scheduler,
native-call driver, shared package, or generic recovery framework is installed.

The adapter overrides the native dispatch and report-backed delivery operations:
the delivery copy preserves the core's correlation, state, authority, conflict,
source and receipt checks while delegating native-envelope validation to the
current-schema observation check. The core itself remains unchanged. Named-step
refusals share the existing durable-consumption ledger, including unreadable state.
